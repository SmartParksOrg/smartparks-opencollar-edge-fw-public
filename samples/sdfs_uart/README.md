# SDFS UART Sample

A basic sample showing how to use the `sdfs_uart_handling` module, which communicates with the
external AK-SDFS-UART (SD card handling) module to append data to the end of the specified file.
