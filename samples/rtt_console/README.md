# RTT / UART shell example

## User instructions for RTT console

Use RTT :

```bash
JLinkRTTClient
```

Run command:

```bash
JLinkExe -device NRF52840_XXAA -if SWD -speed 4000 -autoconnect 1 -usb 801034688
```
